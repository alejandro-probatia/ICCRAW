_Spanish version: [ISSUES.es.md](ISSUES.es.md)_

# Prioritized Issues

This backlog summarizes the implementation plan. The technical justification and
Complete acceptance criteria are in:

- [Operational review and professionalization plan] (OPERATIVE_REVIEW_PLAN.md)

This document is the canonical planning source. GitHub issues
They are its operational reflection and must link to the corresponding sections.

## P0

1. [x] Strictly validate recipes and remove silent mappings from algorithms
   RAW not supported by the active backend.
2. [x] Fix `audit_linear_tiff` to be written before pitch curves,
   conversions or output rendering.
3. [x] Separate the modes in `batch-develop`:
   - assign input ICC profile to RGB camera,
   - convert with real CMM to an output profile.
4. [x] Integrate a real CMM for ICC conversions and leave the side matrix as
   diagnosis, not as the main output.
5. [x] Validate the actual ICC profile generated by ArgyllCMS, not just the matrix of the
   sidecar `.profile.json`.
6. [ ] Add real RAW/DNG dataset with clear license and checksums for testing
   integration.
7. [ ] Ensure ArgyllCMS (`colprof`) and external tools in CI for testing
   of real integration.

## P11. [x] Make the card detection fallback blocking by default or have
   low confidence.
2. [x] Add assisted manual mode for marking chart corners in CLI and GUI.
3. [x] Apply complete sampling parameters from recipe (`trim_percent`,
   `reject_saturated`, patch margin, exclusion criteria).
4. [x] Validate illuminant, observer, source and version of the chart reference.
5. [x] Implement cross-validation with captures not used to build profile.
6. [x] Improve automatic detection of ColorChecker24 in non-ideal conditions.
7. [ ] Complete IT8 support (detection + reference + validation).
8. [x] Add full CGATS export for external interoperability.
9. [x] Add non-synthetic ColorChecker 2005 D50 reference for operational flow.
10. [x] Add scientific development profile prior to the ICC.
11. [x] Execute `auto-profile-batch` in double pass: base recipe -> calibrated recipe -> ICC.
12. [x] Reorganize GUI as flow per file: advanced profile with letter,
    manual basic profile, backpack by RAW and copy/paste of settings.
13. [x] Integrate manual detections saved by capture in `auto-profile-batch`.
14. [ ] Add sharpness/MTF and local contrast QA with measurable criteria, not subjective slider.

## P2

1. Validate pipeline determinism in repeated executions.
2. Performance benchmark and batch parallelization.
3. Reproducible packaging (Linux/Windows installers; pending wheel/container).
4. Scientific contribution guide (capture, lighting, colorimetric QA).
5. [x] Automatic smoke tests for Qt GUI in local headless mode.
6. Bring the smoke GUI to cross-platform CI.
7. Automate license audit and notices for AGPL releases.

## P3

1. Integrate signed C2PA/CAI manifests for chain of custody of the process.
2. Advanced LUT profiling in addition to 3x3 matrix.
3. Automatic comparator between different session/illuminant profiles.
4. GUI internationalization (es/en) and technical presets by discipline.