_Spanish version: [0.3.18.es.md](0.3.18.es.md)_

# ProbRAW 0.3.18

ProbRAW 0.3.18 is a hotfix for undoing viewer geometry changes after recropping
or leveling a preview.

## Fixes

- Undo/redo now distinguishes viewer-only geometry changes from changes that
  affect pixel rendering.
- Going back after selecting a crop no longer invalidates the preview cache or
  forces a final preview rebuild when only crop/rotation state changed.

## Verification

- Full suite: `python -m pytest tests` -> `439 passed, 2 warnings`.
- Targeted regression: `pytest tests/test_gui_session_paths.py -q -k
  "edit_undo_crop_only or edit_menu_undo_redo or edit_clear_adjustments or
  image_crop"` -> `6 passed`.
- Python compilation: `python -m compileall src/probraw/ui/window/edit_history.py`.

## Expected Artifacts

- `ProbRAW-0.3.18-Setup.exe`
- `ProbRAW-0.3.18-Setup.exe.sha256`
- `probraw-0.3.18.tar.gz`
- `probraw-0.3.18-py3-none-any.whl`
- `probraw_0.3.18_python_artifacts.sha256`
